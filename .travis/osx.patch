diff --git a/.travis.yml b/.travis.yml
index 58c24d8..4ce6a17 100644
--- a/.travis.yml
+++ b/.travis.yml
@@ -50,10 +50,10 @@ jobs:
       addons: {"apt":{"sources":[{"sourceline":"deb http://ppa.launchpad.net/hvr/ghc/ubuntu xenial main","key_url":"https://keyserver.ubuntu.com/pks/lookup?op=get&search=0x063dab2bdc0b3f9fcebc378bff3aeacef6f88286"}],"packages":["ghc-8.2.2","cabal-install-3.4","libgtk2.0-dev"]}}
       os: linux
     - compiler: ghc-8.10.2
-      addons: {"apt":{"sources":[{"sourceline":"deb http://ppa.launchpad.net/hvr/ghc/ubuntu xenial main","key_url":"https://keyserver.ubuntu.com/pks/lookup?op=get&search=0x063dab2bdc0b3f9fcebc378bff3aeacef6f88286"}],"packages":["ghc-8.10.2","cabal-install-3.4","libgtk2.0-dev"]}}
+      addons: {"homebrew": {"packages":["gtk+", "gtk-mac-integration", "python3"], "update": true}}
       os: osx
     - compiler: ghc-8.8.4
-      addons: {"apt":{"sources":[{"sourceline":"deb http://ppa.launchpad.net/hvr/ghc/ubuntu xenial main","key_url":"https://keyserver.ubuntu.com/pks/lookup?op=get&search=0x063dab2bdc0b3f9fcebc378bff3aeacef6f88286"}],"packages":["ghc-8.8.4","cabal-install-3.4","libgtk2.0-dev"]}}
+      addons: {"homebrew": {"packages":["gtk+", "gtk-mac-integration", "python3"], "update": true}}
       os: osx
 before_install:
   - HC=$(echo "/opt/$CC/bin/ghc" | sed 's/-/\//')
@@ -108,6 +108,10 @@ install:
     echo "packages: ." >> cabal.project
   - echo 'package threadscope' >> cabal.project
   - "echo '  ghc-options: -Werror=missing-methods' >> cabal.project"
+  - |
+    if [ "$TRAVIS_OS_NAME" = "osx" ]; then
+      echo "constraints: gtk +have-quartz-gtk" >> cabal.project
+    fi
   - |
   - "for pkg in $($HCPKG list --simple-output); do echo $pkg | sed 's/-[^-]*$//' | (grep -vE -- '^(threadscope)$' || true) | sed 's/^/constraints: /' | sed 's/$/ installed/' >> cabal.project.local; done"
   - cat cabal.project || true
@@ -135,6 +139,10 @@ script:
     echo "packages: ${PKGDIR_threadscope}" >> cabal.project
   - echo 'package threadscope' >> cabal.project
   - "echo '  ghc-options: -Werror=missing-methods' >> cabal.project"
+  - |
+    if [ "$TRAVIS_OS_NAME" = "osx" ]; then
+      echo "constraints: gtk +have-quartz-gtk" >> cabal.project
+    fi
   - |
   - "for pkg in $($HCPKG list --simple-output); do echo $pkg | sed 's/-[^-]*$//' | (grep -vE -- '^(threadscope)$' || true) | sed 's/^/constraints: /' | sed 's/$/ installed/' >> cabal.project.local; done"
   - cat cabal.project || true
